import { TitlePrefixPipe } from './title-prefix-pipe';

describe('TitlePrefixPipe', () => {
  it('create an instance', () => {
    const pipe = new TitlePrefixPipe();
    expect(pipe).toBeTruthy();
  });
});
