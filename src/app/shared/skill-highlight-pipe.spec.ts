import { SkillHighlightPipe } from './skill-highlight-pipe';

describe('SkillHighlightPipe', () => {
  it('create an instance', () => {
    const pipe = new SkillHighlightPipe();
    expect(pipe).toBeTruthy();
  });
});
